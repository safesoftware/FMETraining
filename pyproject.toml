[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "browser: marks tests requiring Playwright (deselect with -m 'not browser')",
    "live_mirror: hits the real public S3 content mirror over the network (deselect with -m 'not live_mirror'; auto-skips when offline)",
]
