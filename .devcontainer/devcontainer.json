{
  "name": "FME Training Automation",
  "build": { "dockerfile": "Dockerfile" },
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "moby": false
    }
  },
  "mounts": [
    "source=${localEnv:CLAUDE_DIR},target=/home/vscode/.claude,type=bind,consistency=cached",
    "source=fme-training-claude-plugins,target=/home/vscode/.claude/plugins/marketplaces,type=volume"
  ],
  "postCreateCommand": "pip install -r requirements.txt && npm install -g @anthropic-ai/claude-code && ln -sf /home/vscode/.claude/.claude.json /home/vscode/.claude.json && claude plugin install code-review@claude-plugins-official --scope project || true && claude plugin install feature-dev@claude-plugins-official --scope project || true",
  "postStartCommand": "bash .devcontainer/fix-windows-paths.sh",
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Report server",
      "onAutoForward": "notify"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "anthropic.claude-code"
      ]
    }
  }
}
